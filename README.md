# TDenginex

**TDengine Connector for Elixir**

## Installation

Add TDenginex to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:tdenginex, "> 0.1.0"}
  ]
end
```
run `mix deps.get`.
